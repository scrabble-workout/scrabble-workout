version: 2
jobs:
  test:
    docker:
      - image: circleci/node:10.14.1
    steps:
      - checkout
      - run: ls -larth
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package-lock.json" }}
          - v1-dependencies-
      - run: npm install
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package-lock.lock" }}
      - run: npm run test
      - persist_to_workspace:
          root: .
          paths:
            - build

  github-pages:
    machine:
      enabled: true
    steps:
      - attach_workspace:
          at: .
      - add_ssh_keys:
          fingerprints:
            - "75:11:76:39:d3:aa:28:39:5a:ff:8f:6b:a6:75:1f:4f"
      - run:
          name: Push to github-pages
          no_output_timeout: 1m
          command: |
            git config --global user.email "builds@circleci.com"
            git config --global user.name "CircleCI"
            npm run gh-pages

workflows:
  version: 2
  test-deploy:
    jobs:
      - test
      - github-pages:
          requires:
            - test
          filters:
            branches:
              only: master
